import { LandingPage } from "../src/app/components/LandingPage/LandingPage";

/** Original landing page (v1) — preserved for reference and A/B comparison. */
export default function LandingPageV1Route() {
  return <LandingPage />;
}
