import { LandingPage } from "../src/app/components/LandingPage/LandingPage";

/** App entry: public marketing landing (sign-in via modal). */
export default function Home() {
  return <LandingPage />;
}
